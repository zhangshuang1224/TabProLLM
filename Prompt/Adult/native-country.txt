请你生成 30162 条记录补充在原数据集后面，生成字段包括 native-country，并保持以下类别分布结构。

数据生成过程应遵循以下关键流程：

1️.首先根据目标类别分布，生成各列数据，确保每一列类别分布严格符合给定目标比例

2.对生成数据施加边界限制，确保所有类别符合定义，且数据中不得包含缺失值（NaN）

3.允许通过多轮迭代对数据进行调整优化

4.最终生成的数据应保持所有类别限定在指定集合内，不能出现未定义类别，且不得包含缺失值（NaN）。

5.将生成的 native-country 列按行对齐补充到原数据集中，作为新增字段 native-country，保持原数据行数不变，不得新增行或改变原数据行顺序。

#### 变量 `native-country` 的类别分布：
- 91.188% 的数据类别为 `United-States`；
- 2.022% 的数据类别为 `Mexico`；
- 0.623% 的数据类别为 `Philippines`；
- 0.424% 的数据类别为 `Germany`；
- 0.361% 的数据类别为 `Puerto-Rico`；
- 0.355% 的数据类别为 `Canada`；
- 0.332% 的数据类别为 `India`；
- 0.332% 的数据类别为 `El-Salvador`；
- 0.305% 的数据类别为 `Cuba`；
- 0.285% 的数据类别为 `England`；
- 0.265% 的数据类别为 `Jamaica`；
- 0.235% 的数据类别为 `South`；
- 0.225% 的数据类别为 `China`；
- 0.225% 的数据类别为 `Italy`；
- 0.222% 的数据类别为 `Dominican-Republic`；
- 0.212% 的数据类别为 `Vietnam`；
- 0.209% 的数据类别为 `Guatemala`；
- 0.196% 的数据类别为 `Japan`；
- 0.186% 的数据类别为 `Poland`；
- 0.186% 的数据类别为 `Columbia`；
- 0.139% 的数据类别为 `Iran`；
- 0.139% 的数据类别为 `Taiwan`；
- 0.139% 的数据类别为 `Haiti`；
- 0.113% 的数据类别为 `Portugal`；
- 0.109% 的数据类别为 `Nicaragua`；
- 0.099% 的数据类别为 `Peru`；
- 0.096% 的数据类别为 `Greece`；
- 0.09% 的数据类别为 `France`；
- 0.09% 的数据类别为 `Ecuador`；
- 0.08% 的数据类别为 `Ireland`；
- 0.063% 的数据类别为 `Hong`；
- 0.06% 的数据类别为 `Cambodia`；
- 0.06% 的数据类别为 `Trinadad&Tobago`；
- 0.056% 的数据类别为 `Thailand`；
- 0.056% 的数据类别为 `Laos`；
- 0.053% 的数据类别为 `Yugoslavia`；
- 0.046% 的数据类别为 `Outlying-US(Guam-USVI-etc)`；
- 0.043% 的数据类别为 `Hungary`；
- 0.04% 的数据类别为 `Honduras`；
- 0.036% 的数据类别为 `Scotland`；
- 0.003% 的数据类别为 `Holand-Netherlands`；
所有生成的数据，必须限定在上述类别集合内，且类别分布应严格匹配。
